#include <LittlePhysics/CollisionManager.h>

namespace LP {

	/*uint32 CollisionManager::AddBody(Body* body)
	{
		Shape* shape;
		body->GetShape(shape);
		return m_DbvhTree.Insert(body, shape->GetAABB(body->GetTransform()));
	}

	void CollisionManager::RemoveBody(uint32 handle)
	{
		m_DbvhTree.Remove(handle);
	}

	void CollisionManager::Update()
	{
		
	}

	void CollisionManager::Collide()
	{
		m_DbvhTree.TestCollision();
	}*/

}